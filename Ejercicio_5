/*5- Obtener la suma de los primeros 8 elementos de la siguiente serie:
 Z1: ((1/5)*1) Z2: ((2/5)*2) Z3: ((3/5)*4) Z4: ((4/5)*8) Usar ciclo: while o for.*/

#include <stdio.h>

int main() 
    {
        double s = 0.0;
        double n = 1.0;
        double d = 5.0;
        double m = 1.0;
        int i = 1; //contador

        while (i <= 8) {
        s += (n / d) * m;
        n++;
        m *= 2.0;
        i++;
    }

    printf("La suma de los primeros 8 elementos de la serie es: %.2f\n", s);

    return 0;
}
